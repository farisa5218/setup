#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
    int n,a,b,x;
    scanf("%d%d%d",&n,&a,&b);
    double abs(ans) =sqrt(n);
    x=pow(a,b);
    printf("%.2lf %d",ans,x);
    return 0;
}

